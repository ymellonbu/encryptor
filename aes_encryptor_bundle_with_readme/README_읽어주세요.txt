🔐 실행 안내 / Execution Instructions / 运行说明 / 執行說明
────────────────────────────────────────────
(KO) 한국어 [KO]:
1. `dist` 폴더로 들어갑니다.  
2. `.exe` 확장자를 가진 파일을 실행하세요.  
   → 암호화 프로그램이 시작됩니다.

────────────────────────────────────────────
(EN) English [EN]:
1. Open the `dist` folder.  
2. Run the file with the `.exe` extension.  
   → The encryption tool will launch.

────────────────────────────────────────────
(ZH-CN) 简体中文 [ZH-CN]:
1. 打开 `dist` 文件夹。  
2. 运行扩展名为 `.exe` 的文件。  
   → 加密程序将会启动。

────────────────────────────────────────────
(ZH-TW) 繁體中文 [ZH-TW]:
1. 打開 `dist` 資料夾。  
2. 執行副檔名為 `.exe` 的檔案。  
   → 加密程式將會啟動。

⚠️ 암호화 도구 사용 주의사항 / Important Notice / 注意事项
────────────────────────────────────────────
(KO) 한국어:
이 프로그램은 파일 또는 폴더를 암호화 및 복호화하는 도구입니다.

1. 복호화에는 동일한 키가 필요합니다.  
2. 잘못된 키를 사용하면 파일이 손상될 수 있습니다.  
3. 사용 전 반드시 원본 파일을 백업하세요.  
4. 악용은 금지되며, 모든 책임은 사용자에게 있습니다.

────────────────────────────────────────────
(EN) English:
This tool encrypts or decrypts files and folders using a password.

1. The same key is required for decryption.  
2. Using the wrong key may corrupt your files.  
3. Always back up your original files before use.  
4. Misuse is strictly prohibited. You are fully responsible for any consequences.

────────────────────────────────────────────
(ZH-CN) 简体中文:
该程序用于加密和解密文件或文件夹。

1. 解密需使用完全相同的密钥。  
2. 密钥错误可能导致文件损坏。  
3. 使用前请务必备份原始文件。  
4. 禁止滥用，后果由用户自负。

────────────────────────────────────────────
(ZH-TW) 繁體中文:
此程式可用於加密與解密檔案或資料夾。

1. 解密時需使用完全相同的密碼。  
2. 錯誤的密碼可能會損毀檔案。  
3. 使用前請先備份原始檔案。  
4. 禁止惡意使用，一切後果由使用者自行承擔。
